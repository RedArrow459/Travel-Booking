<!DOCTYPE html>
<html>
<head>
    <title>Travel Booking Website</title>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.3/dist/leaflet.css" />
    <script src="https://unpkg.com/leaflet@1.9.3/dist/leaflet.js"></script>
    <style>
        #map {
            width: 100%;
            height: 400px;
        }
    </style>
</head>
<body>
    <h1>Travel Booking</h1>

    <div id="map"></div>

    <h2>Hotel Booking</h2>
    <form>
        <label for="hotel-city">City:</label>
        <input type="text" id="hotel-city">
        <label for="hotel-checkin">Check-in:</label>
        <input type="date" id="hotel-checkin">
        <label for="hotel-checkout">Check-out:</label>
        <input type="date" id="hotel-checkout">
        <button type="submit">Search Hotels</button>
    </form>

    <h2>Car Rental Booking</h2>
    <form>
        <label for="car-city">City:</label>
        <input type="text" id="car-city">
        <label for="car-pickup">Pickup Date:</label>
        <input type="date" id="car-pickup">
        <label for="car-return">Return Date:</label>
        <input type="date" id="car-return">
        <button type="submit">Search Cars</button>
    </form>

    <h2>Feedback Form</h2>
    <form>
        <label for="feedback-message">Your Feedback:</label>
        <textarea id="feedback-message"></textarea>
        <button type="submit">Submit Feedback</button>
    </form>

    <script>
        var map = L.map('map').setView([28.6139, 77.2090], 13); // Adjust latitude and longitude as needed

        L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
            attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'
        }).addTo(map);

        // Add Leaflet markers for hotels and car rental locations based on search results
        // Implement logic to fetch data from a backend API or database and display it on the map

        // Implement form submission logic to send feedback data to a backend endpoint
    </script>
</body>
</html>